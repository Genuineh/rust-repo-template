# Prompt 模板（示例）

## 1) 生成复现用例（Bug）
```
我有一个 Rust 函数，输入和预期输出如下：
<粘贴代码>
请帮我生成一个最小可复现的测试用例（`#[test]`），并解释可能出错的原因。输出只包含测试代码与简短解释。
```

## 2) 生成单元测试（Feature）
```
请为以下函数生成 3 个边界条件的单元测试：
<粘贴函数>
```

## 3) 重构并附带测试
```
请重构下面的函数以提高可读性与性能，并为其生成单元测试：
<粘贴函数>
```

> 使用这些 prompt 时，请在 PR 中附上你使用的 prompt 与 LLM 的输出。