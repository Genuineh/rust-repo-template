#!/usr/bin/env bash
# Example custom docs hook
set -euo pipefail
echo "Running custom docs hook (example)"
# Example: run link checkers or deploy docs to a staging site
# cargo install --locked mdbook-linkcheck || true
# mdbook-linkcheck book || true
exit 0