/*
Example integration test (commented out so the template builds without needing changes).

Replace `your_crate_name` with the crate name (from `Cargo.toml`) and uncomment.

use your_crate_name::add;

#[test]
fn add_works() {
    assert_eq!(add(2, 3), 5);
}
*/