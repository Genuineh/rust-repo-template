title: "Template: initialize project"

This task represents initializing the generated project from the template.

- Create initial files
- Add CI and docs
- Add plan tasks


# history
- A `history/` directory is created alongside this `task.md` for recording events (e.g. reviews, CI notes).